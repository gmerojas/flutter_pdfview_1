name: flutter_pdf
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  #flutter_pdfview: ^1.3.2
  flutter_pdfview: ^1.1.0
  #flutter_pdfview: ^1.4.4
  path_provider: ^2.1.1  # Para URLs
  http: ^1.1.0          # Para descargar URL
  #downloads_path_provider_28: ^0.1.2  # ← Para carpeta Descargas
  permission_handler: ^11.0.1         # ← Para permisos
  device_info_plus: ^10.1.0  # ← Para detectar versión Android

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/sample.pdf
